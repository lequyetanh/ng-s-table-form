import { CurrencyNumberDirective } from './currency-number.directive';

describe('CurrencyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
